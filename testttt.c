#include <stdio.h>
int main()
{
    int x = 5, y = 2;
    char op = '+';
    switch (op)
        % 3D
        {
        case '+':
            x += y;
        case '*':
            x *= y;
            break;
        default:
            x += 1;
        }
}