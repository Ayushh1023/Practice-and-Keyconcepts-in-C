#include <stdio.h>
int main()
{
    // one line code below
    // char st[34]; printf("Enter your name::"); gets(st);  printf("The name is "); puts(st); return 0;
    char st[34];
    // printf("Enter your name::");
    gets(st);
    // printf("The name is ");
    puts(st);
    return 0;
}